{
  "name": "vzc1-relay-server",
  "version": "1.0.0",
  "description": "VZC1 relay server (WebSocket + HTTP) for controller <-> phone",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ws": "^8.13.0"
  }
}